# research

This repository contains a proof-of-concept Go web UI located in [`go-web-ui/`](go-web-ui/).

## Running the demo

```bash
cd go-web-ui
go run .
```

The server binds to an ephemeral loopback port and logs the admin token along with the URL for the UI.
